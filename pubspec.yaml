name: package_samples
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  pedantic_mono:
  hooks_riverpod: ^1.0.3
  freezed_annotation: ^2.1.0
  expandable: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.2.0
  freezed: ^2.1.0+1

flutter:
  uses-material-design: true

